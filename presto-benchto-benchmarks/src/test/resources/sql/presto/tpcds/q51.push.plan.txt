local exchange (GATHER, SINGLE, [])
    remote exchange (GATHER, SINGLE, [])
        local exchange (GATHER, SINGLE, [])
            remote exchange (REPARTITION, HASH, ["expr_68"])
                join (FULL, PARTITIONED):
                    local exchange (GATHER, SINGLE, [])
                        remote exchange (REPARTITION, HASH, ["ss_item_sk"])
                            final hashaggregation over (d_date_19, ss_item_sk)
                                local exchange (GATHER, SINGLE, [])
                                    remote exchange (REPARTITION, HASH, ["d_date_19", "ss_item_sk"])
                                        partial hashaggregation over (d_date_19, ss_item_sk)
                                            join (INNER, REPLICATED):
                                                scan store_sales (pushdown = true)
                                                local exchange (GATHER, SINGLE, [])
                                                    remote exchange (REPLICATE, BROADCAST, [])
                                                        scan date_dim (pushdown = true)
                    local exchange (GATHER, SINGLE, [])
                        remote exchange (REPARTITION, HASH, ["ws_item_sk"])
                            final hashaggregation over (d_date, ws_item_sk)
                                local exchange (GATHER, SINGLE, [])
                                    remote exchange (REPARTITION, HASH, ["d_date", "ws_item_sk"])
                                        partial hashaggregation over (d_date, ws_item_sk)
                                            join (INNER, REPLICATED):
                                                scan web_sales (pushdown = true)
                                                local exchange (GATHER, SINGLE, [])
                                                    remote exchange (REPLICATE, BROADCAST, [])
                                                        scan date_dim (pushdown = true)
